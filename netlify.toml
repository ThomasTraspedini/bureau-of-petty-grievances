[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24"
  NPM_VERSION = "11.19.0"

# Runtime secrets and AWS_LAMBDA_JS_RUNTIME belong in the Netlify UI.
# Next.js support is supplied by Netlify's automatic adapter.
